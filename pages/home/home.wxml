<!--pages/home/home.wxml-->
<view>数据绑定</view>
<text>{{msg}}</text>
<view>列表渲染</view>
<block wx:for="{{list}}" wx:key="id">
  <view>{{index}} -- {{item.name}}</view>
</block>
<view>事件监听</view>
<view>{{conter}}</view>
<button size='mini' bindtap='onAdd'>+</button>
<button size='mini' bindtap='onDiff'>-</button>